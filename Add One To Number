vector<int> Solution::plusOne(vector<int> &A) {
   A.insert(A.begin(),0);
   int n = A.size();
   for(int i=n-1;i>=0;i--)
   {
       if(A[i]<9)
       {
           A[i]++;
       }else
       A[i]=0;
       break;
   }
   while(A[0]==0 && A[i]<9)
   {
       
   }
   
